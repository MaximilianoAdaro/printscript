package token;

public enum TokenType {
    LET,

    IDENTIFIER,

    NUMBER_TYPE,
    STRING_TYPE,

    TYPE_DECLARATION,

    STRING,
    NUMBER,

    SEMICOLON,
    ASSIGNATION,

    PRODUCT,
    SUM,
    MINUS,
    DIVISION,

    PRINT,
}
