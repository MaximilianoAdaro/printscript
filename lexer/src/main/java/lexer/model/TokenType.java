package lexer.model;

public enum TokenType {
    SEMICOLON,
    COLON,
    ASSIGNATION,
    LEFT_PAREN,
    RIGHT_PAREN,

    MINUS,
    PLUS,
    MULTIPLY,
    DIVIDE,

    IDENTIFIER,

    STRING,
    NUMBER,

    PRINT,
    LET,

    NUMBER_TYPE,
    STRING_TYPE,
}
